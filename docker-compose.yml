services:
  db:
    image: postgres:16
    container_name: postgres-db
    environment:
      POSTGRES_DB: socio
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: adminpassword
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:   
  postgres_data:
